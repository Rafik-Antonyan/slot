export interface ISlotData {
    betValue: number;
    balance: number;
    autoSpins: number;
    autoBonusRounds: number;
    isAutoBonus: boolean;
}

export interface IMenuItem {
    image: string;
    label: string;
    selected: boolean;
}
